<!-- <view id="home">
  <view class="top">
    <view class="top-box">
     
      <view class="bulletin-box">
        <view class="bulletin">欢迎加入yizo校园</view>
      </view>
    </view>
  </view>
  <view class="list">
    <menu v-bind:menu.once="{{navs.nav[navs.current]}}"></menu>
    <runOrdersPost />
  </view>
</view> -->


<view id="home">
  <view class="background">
    <view class="top"/>
  </view>
  <view class="main">
    <view class="top">
        <view class="college">
            <image src="{{college.logo}}" class="logo"/>
            <view class="info">
              <view class="name">{{college.college_name}}</view>
              <view class="mes">
                <text>人气:100</text>
                <text>昨日新增:1000</text>
              </view>
            </view>
          </view>
          <view class="bulletin-box">
            <view class="iconfont icon-setting bulletin">欢迎加入yizo校园</view>
          </view>
    </view>
    <view class="menu">
  <view class="menus">
    <block wx:for="{{$menu$menus}}" wx:for-index="$menu$menuIndex" wx:for-item="$menu$menu" wx:key="$menu$unique">
      <view class="nav-item {{$menu$current==$menu$menuIndex?'active':''}}" bindtap="$menu$onSwitch" data-type="menu" data-key="{{$menu$menu.key}}" data-index="{{$menu$menuIndex}}">
        <text class="title">{{$menu$menu.name}}</text>
      </view>
    </block>
  </view>
  <view class="sub-menus">
    <block wx:for="{{$menu$menus[$menu$current].subMenus}}" wx:for-index="$menu$subIndex" wx:for-item="$menu$subMenu" wx:key="$menu$unique">
      <view class="sub-nav-item {{$menu$menus[$menu$current].current==$menu$subIndex?'active':''}}" bindtap="$menu$onSwitch" data-key="{{$menu$subMenu.key}}" data-type="sub" data-index="{{$menu$subIndex}}">
        <text class="title">{{$menu$subMenu.name}}</text>
      </view>
    </block>
  </view>
</view>
    <view class="list">
        
<view class="run-order-list">
  <view class="app-toast {{$runOrdersPost$toast$mask?'app-toast-mask':''}}"  wx:if="{{$runOrdersPost$toast$show}}">
  <view style="display: inline-block">
    <view class="app-toast-box ">
      <view class="toast-icon">
        <view class="loading" wx:if="{{$runOrdersPost$toast$icon=='loading'}}">
  <view style="left: 0;top: 0;"/>
  <view style="right: 0;top: 0;animation-delay: .4s;"/>
  <view style=" right: 0;bottom: 0;animation-delay: .8s;"/>
  <view style=" left: 0; bottom: 0;animation-delay: 1.2s;"/>
</view>
        <block wx:else>
          <view class="iconfont icon-{{$runOrdersPost$toast$icon}}"/>
        </block>
      </view>
      <view class="toast-content">{{$runOrdersPost$toast$title}}</view>
    </view>
  </view>
</view>
  <block wx:for="{{$runOrdersPost$orderList}}" wx:for-index="$runOrdersPost$index" wx:for-item="$runOrdersPost$orderItem" wx:key="$runOrdersPost$index">
    <view class="order-item" hover-class="order-item-active" bindtap="$runOrdersPost$onView" data-id="{{$runOrdersPost$orderItem.order_id}}">
      <block wx:if="{{$runOrdersPost$orderItem.gender_constraint !== $runOrdersPost$null}}">
        <view class="gender-constraint">
          <block wx:if="{{$runOrdersPost$orderItem.gender_constraint == 0}}">
            <view class="ribbon-wrapper">
              <view class="ribbon" style="background-color: #FF0852;">限女生</view>
            </view>
          </block>
          <block wx:elif="{{$runOrdersPost$orderItem.gender_constraint == 1}}">
            <view class="ribbon-wrapper">
              <view class="ribbon" style="background-color: #1C79FF;">限男生</view>
            </view>
          </block>
        </view>
      </block>
      <view class="item-top">
        
  {{$runOrdersPost$orderItem.creater}}
  <image src="{{$runOrdersPost$orderItem.creater?('https://localhost:3000/account/avatar/'+$runOrdersPost$orderItem.creater+'.png'):$runOrdersPost$avatar$url}}" class="avatar"/>

        <view class="info">
          <view>
            <text class="username">{{$runOrdersPost$orderItem.creater_name}}</text>
            <view class="iconfont icon-{{$runOrdersPost$orderItem.creater_gender==0?'female':'male'}}"/>
          </view>
          <view>
            <text class="time">{{$runOrdersPost$orderItem._c}}</text>
          </view>
        </view>
      </view>
      <view class="item-content">
        
        <text class="reward" wx:if="{{$runOrdersPost$orderItem['reward_type']==0}}">{{$runOrdersPost$orderItem.reward}}</text>
        <text class="reward reward-money" wx:else>￥{{$runOrdersPost$orderItem.reward}}</text>
        <view class="title ">{{$runOrdersPost$orderItem.title}}</view>
        <view class="address">
          <view class="iconfont icon-point"/>
          {{$runOrdersPost$orderItem.address}}
        </view>
      </view>
    </view>
  </block>
</view>
    </view>
  </view>
</view>